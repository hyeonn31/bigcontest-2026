# bigcontest/ — 2026 빅콘테스트 프로젝트

`raw/wiki/Output` 세컨드 브레인 구조와는 별개인 독립 프로젝트 작업공간. 참가 리그: **AI데이터분석 — 기후위기 취약상권 영향 분석**. 대회 배경과 데이터 스키마, 진행 상황은 [notes.md](notes.md) 참고.

## 폴더 구조

- `data/` — 원본 및 가공 데이터
  - `sk_flow_pop/`, `shinhan_card/`, `weather/` — 출처별 원본 데이터
  - `processed/` — 집계·가공된 중간 산출물 (날짜×지역 단위 등)
- `scripts/` — 분석/집계 파이썬 스크립트
- `figures/` — 그래프 이미지
- `eda_output/` — EDA 텍스트 결과물

## 인코딩 규칙 (중요)

**이 프로젝트의 모든 데이터 파일은 UTF-8(BOM)로 통일한다.**

- 공공기관/기업이 제공하는 한국어 CSV·TXT는 대부분 원본이 CP949(EUC-KR)라, 받는 즉시 UTF-8로 변환해서 저장한다 (원본을 덮어써도 무방 — 이 프로젝트 데이터는 `raw/`의 불변 원본 규칙 대상이 아님).
- 변환 후에는 관련 스크립트의 `encoding='cp949'`도 `encoding='utf-8-sig'`로 함께 맞춘다.
- 이렇게 하는 이유: CP949 상태로 두면 사용자 에디터에서 한글이 깨져 보이고(한자처럼 보이는 증상), 매번 인코딩을 신경 써야 해서 번거로움 — 처음부터 통일해서 이 문제를 없앤다.
- 지금까지 변환 완료: `data/shinhan_card/*.txt`, `data/weather/*.csv` (2026-09-18)
